# Public Folder

This folder contains static assets that will be served from the root URL.

## Logo Image

Place your logo image here as `logo.png` (or update the path in `src/components/Navbar.jsx`).

Supported formats: PNG, JPG, SVG, etc.

If no logo image is found, the navbar will display "SR" as a placeholder.



